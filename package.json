{
  "name": "permission",
  "version": "0.0.0",
  "description": "Creates a thin access control layer that can be used by other (for example the user) modules.",
  "scripts": {
    "version": "(cd ../../ && pos-cli modules version permission -p) && git add template-values.json && auto-changelog -p && git add CHANGELOG.md"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Platform-OS/pos-module-permission.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Platform-OS/pos-module-permission/issues"
  },
  "homepage": "https://github.com/Platform-OS/pos-module-permission#readme",
  "devDependencies": {
    "auto-changelog": "^2.4.0"
  },
  "auto-changelog": {
    "template": "changelog-template.hbs",
    "unreleased": true,
    "commitLimit": false
  }
}
